---
title: Truncamiento de texto · Bootstrap en Español v5.3
description: Truncar largas cadenas de texto con puntos suspensivos.
noindex: true
---
import { MainBanner } from '@components/main-banner'
import { ContentSuggestion } from '@components/content-suggestion'
import { DemoIframe } from '@components/demo-iframe'
import { Callout } from 'nextra/components'

# Truncamiento de texto

Truncar largas cadenas de texto con puntos suspensivos.

<MainBanner />

Para contenido más extenso, puedes agregar una clase `.text-truncate` para truncar el texto con puntos suspensivos. **Requiere `display: inline-block` o `display: block`.**

<DemoIframe path="/demos/bootstrap/5.3/helpers/text-truncation/index.html">
    ```html filename="HTML"
    <!-- Block level -->
    <div class="row">
        <div class="col-2 text-truncate">
            Este texto es bastante largo y se truncará una vez mostrado.
        </div>
    </div>

    <!-- Inline level -->
    <span class="d-inline-block text-truncate" style="max-width: 150px;">
        Este texto es bastante largo y se truncará una vez mostrado.
    </span>
    ```
</DemoIframe>
